[project]
name = "nltk-nlp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ricardo Santos", email = "ricardobatista28@outlook.com" }
]
requires-python = ">=3.10"
dependencies = [
    "nltk>=3.10.2",
]

[project.scripts]
nltk-nlp = "nltk_nlp:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
