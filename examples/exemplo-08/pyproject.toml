[project]
name = "exemplo-08"
version = "0.1.0"
description = "Embeddings e Busca Vetorial"
requires-python = ">=3.10"
dependencies = [

]

[tool.hatch.build.targets.wheel]
packages = ["nltk_nlp"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
