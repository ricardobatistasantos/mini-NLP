[project]
name = "exemplo-02"
version = "0.1.0"
description = "Normalização de Texto"
requires-python = ">=3.10"
dependencies = [
    "nltk>=3.10.2",
]

[tool.hatch.build.targets.wheel]
packages = ["nltk_nlp"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
