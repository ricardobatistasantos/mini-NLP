[project]
name = "exemplo-07"
version = "0.1.0"
description = "Busca Lexical com BM25"
requires-python = ">=3.10"
dependencies = [

]

[tool.hatch.build.targets.wheel]
packages = ["nltk_nlp"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
